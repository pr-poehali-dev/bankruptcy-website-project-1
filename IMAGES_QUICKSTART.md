# 🚀 Быстрый старт: Работа с изображениями

## За 30 секунд

### Как добавить изображение на страницу?

```tsx
// 1. Импортируйте компонент
import SafeImage from '@/components/SafeImage';
import { IMAGES } from '@/config/images';

// 2. Используйте в JSX
<SafeImage
  src={IMAGES.hero.main}
  alt="Описание"
  className="w-full h-full object-cover"
  fallbackIcon="Scale"
  fallbackGradient="from-purple-100 to-orange-100"
/>
```

**Готово!** ✅ Изображение загрузится с автоматическим fallback.

---

## Где найти изображения?

### Все доступные категории:

```tsx
IMAGES.hero.main              // Главная страница - герой
IMAGES.about.main             // О компании
IMAGES.ogImage.main           // Social media превью

// Блог (6 статей)
IMAGES.blog.avoidBankruptcy
IMAGES.blog.bankruptcyProcedure
IMAGES.blog.debtForgiveness
IMAGES.blog.financialLiteracy
IMAGES.blog.collectorProtection
IMAGES.blog.legalAlternatives

// Кейсы
IMAGES.cases.successfulIndividual
IMAGES.cases.corporateLiquidation
IMAGES.cases.restructuring

// Команда
IMAGES.team.consultation
IMAGES.team.testimonial
IMAGES.team.specialist
```

📄 **Полный список**: См. `IMAGES_INVENTORY.md`

---

## Fallback иконки - шпаргалка

Выбирайте подходящую иконку из lucide-react:

| Тематика | Иконка | Пример |
|----------|--------|--------|
| Юридическая | `Scale` | Весы правосудия |
| Документы | `FileText` | Блог, статьи |
| Финансы | `DollarSign` | Кейсы, цены |
| Команда | `Users` | О нас |
| Успех | `TrendingUp` | Рост, достижения |
| Безопасность | `Shield` | Защита от коллекторов |

---

## Градиенты - шпаргалка

Стандартные комбинации по брендбуку:

```tsx
// Основной (фиолетово-оранжевый)
fallbackGradient="from-purple-100 to-orange-100"

// Альтернативный (сине-фиолетовый)
fallbackGradient="from-blue-100 to-purple-100"

// Реверсивный (оранжево-фиолетовый)
fallbackGradient="from-orange-100 to-purple-100"

// Нейтральный (серый)
fallbackGradient="from-slate-100 to-gray-100"
```

---

## Что делать если изображение не грузится?

**Ничего!** 🎉 

SafeImage автоматически:
1. Покажет красивую иконку вместо сломанного изображения
2. Сохранит дизайн страницы
3. Не вызовет ошибок в консоли

---

## FAQ

**Q: Могу ли я использовать обычный `<img>`?**  
A: Можно, но не рекомендуется. SafeImage даёт автоматический fallback.

**Q: Где хранятся изображения?**  
A: На CDN Poehali.dev, быстрая загрузка и кеширование.

**Q: Как добавить своё изображение?**  
A: Добавьте URL в `src/config/images.ts` и используйте через SafeImage.

**Q: Нужно ли оптимизировать изображения?**  
A: Нет, CDN автоматически кеширует и оптимизирует загрузку.

---

## Примеры из проекта

### Пример 1: Hero секция
```tsx
<SafeImage
  src={IMAGES.hero.main}
  alt="Банкротство физических и юридических лиц в Перми"
  className="aspect-square w-full h-full object-cover"
  fallbackIcon="Scale"
  fallbackGradient="from-purple-100 to-orange-100"
/>
```

### Пример 2: Блог
```tsx
<SafeImage
  src={post.image}
  alt={post.title}
  className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300"
  fallbackIcon="FileText"
  fallbackGradient="from-purple-100 to-blue-100"
/>
```

### Пример 3: Динамическое изображение
```tsx
<SafeImage
  src={`https://cdn.poehali.dev/projects/.../dynamic-${id}.jpg`}
  alt="Динамическое изображение"
  fallbackIcon="Image"
/>
```

---

**💡 Совет**: Всегда используйте SafeImage вместо обычного `<img>` для надёжности!

**📖 Подробная документация**: `IMAGES.md`  
**📊 Инвентаризация**: `IMAGES_INVENTORY.md`
